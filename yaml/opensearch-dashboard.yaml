---
# Source: opensearch-dashboards/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: RELEASE-NAME-opensearch-dashboards-dashboards
  labels:
    helm.sh/chart: opensearch-dashboards-1.1.0
    app.kubernetes.io/name: opensearch-dashboards
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.1.0"
    app.kubernetes.io/managed-by: Helm
---
# Source: opensearch-dashboards/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: RELEASE-NAME-opensearch-dashboards-config
  labels:
    helm.sh/chart: opensearch-dashboards-1.1.0
    app.kubernetes.io/name: opensearch-dashboards
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.1.0"
    app.kubernetes.io/managed-by: Helm
data:
  opensearch_dashboards.yml: |
    opensearch.password: admin
    opensearch.requestHeadersWhitelist:
    - Authorization
    - security_tenant
    opensearch.ssl.verificationMode: none
    opensearch.username: admin
    opensearch_security.auth.type: openid
    opensearch_security.cookie.secure: true
    opensearch_security.openid.client_id: opensearch
    opensearch_security.openid.client_secret: 7a062e30-f53a-4471-a8b9-edec542d8c57
    opensearch_security.openid.connect_url: http://hyperauth.org/auth/realms/tmax/.well-known/openid-configuration
    server: null
---
# Source: opensearch-dashboards/templates/rolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    helm.sh/chart: opensearch-dashboards-1.1.0
    app.kubernetes.io/name: opensearch-dashboards
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.1.0"
    app.kubernetes.io/managed-by: Helm
  name: RELEASE-NAME-opensearch-dashboards-dashboards-rolebinding
roleRef:
  kind: Role
  name: RELEASE-NAME-opensearch-dashboards-dashboards
  apiGroup: rbac.authorization.k8s.io
subjects:
- kind: ServiceAccount
  name: RELEASE-NAME-opensearch-dashboards-dashboards
  namespace: default
---
# Source: opensearch-dashboards/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-opensearch-dashboards
  labels:
    helm.sh/chart: opensearch-dashboards-1.1.0
    app.kubernetes.io/name: opensearch-dashboards
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
  - port: 5601
    protocol: TCP
    name: http
    targetPort: 5601
  selector:
    app: opensearch-dashboards
    release: "RELEASE-NAME"
---
# Source: opensearch-dashboards/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: RELEASE-NAME-opensearch-dashboards
  labels:
    helm.sh/chart: opensearch-dashboards-1.1.0
    app.kubernetes.io/name: opensearch-dashboards
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "1.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: opensearch-dashboards
      release: "RELEASE-NAME"
  template:
    metadata:
      labels:
        app: opensearch-dashboards
        release: "RELEASE-NAME"
      annotations:
        configchecksum: 868d4bf0448e08e31d7518662959e8fcd43ce8ed5311459c3cc457877b57261
    spec:
      securityContext:
        {}
      serviceAccountName: RELEASE-NAME-opensearch-dashboards-dashboards
      volumes:
        - name: config
          configMap:
            name: RELEASE-NAME-opensearch-dashboards-config
      containers:
      - name: dashboards
        securityContext:
          capabilities:
            drop:
            - ALL
          runAsNonRoot: true
          runAsUser: 1000
        image: "opensearchproject/opensearch-dashboards:1.0.0"
        imagePullPolicy: "IfNotPresent"
        env:
        - name: OPENSEARCH_HOSTS
          value: "https://opensearch-cluster-master:9200"
        - name: SERVER_HOST
          value: "0.0.0.0"
        ports:
        - containerPort: 5601
          name: http
          protocol: TCP
        resources:
          limits:
            cpu: 100m
            memory: 512M
          requests:
            cpu: 100m
            memory: 512M
        volumeMounts:
          - name: config
            mountPath: /usr/share/opensearch-dashboards/config/opensearch_dashboards.yml
            subPath: opensearch_dashboards.yml
